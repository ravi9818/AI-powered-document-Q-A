# AI-powered-document-Q-A
AI-powered document QA system using RAG. It processes TXT, CSV, and JSON files, converts them into embeddings, retrieves relevant content, and generates accurate answers using LLMs with a Streamlit UI.
